# Tasks: add-live-sources
- [x] 1.1 `loadHttp(baseUrl)` fetches /contract + /events; errors name URL and cause
- [x] 1.2 App UI: add-host-by-URL input; Refresh button for HTTP-origin hosts (per-host atomic replace, failures surfaced without dropping hosts)
- [x] 1.3 End-to-end against a live TBDoctor --serve
