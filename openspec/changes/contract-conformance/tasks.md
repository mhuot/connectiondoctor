# Tasks: contract-conformance
- [ ] 1.1 `docs/fixtures/` with three fault cases and `expected.json`; README in the folder
- [x] 1.1a Negative-control fixtures in `docs/fixtures/` — normal unplug, sleep/wake, KVM move, duplicate VID:PID, incomplete history, shallow deficit — each with `expected.json`, a README stating provenance (constructed vs recorded) and why the answer is right; paired fault cases (sustained deficit, hub branch lost); TS conformance runner. **Scope, precisely:** incident stitching is *executed* against the corpus (the sustained-vs-shallow deficit pair pins both edges of the rule), and the fixtures are validated as contract-valid (UUIDv4 `host.id`, 16-hex `unitKey`). Finding quality is *asserted, not executed* — no TS engine recomputes findings, so those assertions check that each case declares a coherent answer and that any findings its envelope carries agree. **Remaining for this task's original claim:** running the Swift and C# engines over the corpus, which is 1.3/1.4; parity across the three engines is the same follow-up — issue #31
- [x] 1.1c Identity fixtures: `identity-rename`, `identity-hostname-reuse`, `identity-duplicate-docks`. **Executed, not asserted** — they run through the real `loadFiles` + `hostKey`, in both file orders, so identity keying is the second thing this corpus actually exercises alongside incident stitching. They are **consumer** cases by nature (two documents meeting is a situation no producer is in), so 1.3/1.4 parity does not apply and their absence from a producer run is correct. **Not covered:** cross-endpoint unit correlation — the duplicate-dock keys differ by construction and nothing here joins them, which is fleet-integration scope — issue #27
- [ ] 1.1b `docs/schema/v1/{envelope,report,diff,bundle-manifest}.schema.json` (JSON Schema 2020-12) for the documents incl. Finding/Incident/Node (with explicit optional `nameRedacted`, `unitKey`, `host.id`); dashboard parser tests and every conformance test validate against them; manifest test asserts no scope token, installation key, original id/name or mapping appears — issue #20, review of #35
- [ ] 1.2 `docs/schema-v1.md`: the nine rules with why-clauses; `acChanged`; `source` on link events; arch enum; `tb.route`; `host.id` and `node.unitKey` (proposed rows already present) — issue #27
- [x] 1.2b Both producers: `identity.json` in the data dir with a random `host.id` and an `installationKey` (owner-only on macOS, atomic write on Windows); emit `host.id`, and `unitKey` = HMAC(serial, key) where a serial exists (Windows reads the serial from the instance id's last segment, ignoring bus-generated paths); dashboard keys hosts on `host.id` with `host.name` fallback — issue #27
- [ ] 1.2c Share-scoped redaction per `schema-v1.md` § Redaction: `--redact` on `contract`/`report`/`diff` (implicit one-document scope), `bundle <out.zip> [--hours N] [--scope token]` with a generated high-entropy token and a versioned `manifest.json` (`kind: bundle-manifest`, pseudonyms/categories/counts/files/coverage only) that `bundle` also prints as a summary after creation; pseudonymise `host.id`, `host.name` and every `nodes[].id` with all references rewritten recursively (parentId, attachedTo, sharedParent, event nodeId, diff nodes, embedded `fullSnapshot` envelopes); remove `platform{}`, `unitKey`, serials; conservative labels for user-assigned names + `nameRedacted?` on Node, Display, IncidentDevice, Event, Adapter; findings/notes generated from redacted values or via the replacement map before serialization; fixture with a personal name, hostname and native id inside evidence/note text asserting none survive; producers classify user-assigned name fields per platform; tests: validates against JSON Schema, every reference resolves, topology unchanged, no original id/hostname/serial substring survives, one bundle joins, two bundles do not correlate, rename still joins locally, `--scope` reuse warns
- [ ] 1.3 macOS analysis over envelope+events; conformance test
- [ ] 1.4 Windows analysis over envelope+events (vidPid+parent identity — removes the interim `note` from `connection_diff`/`diff`, issue #19; `adapter` always present, displays[], tunneled rule, linkDown/linkUp poll-derived, trim→snapshot); conformance test
- [ ] 1.5 Dashboard: read display nodes with `attachedTo`; conformance test over `domain/`
- [ ] 1.6 `domain/excalidraw.ts` + `build-ui` emits `excalidraw.js`; collectors embed and run it (JavaScriptCore / Jint) for `excalidraw` and `connection_diagram`; delete `Diagram.swift`, `ExcalidrawExport.swift`
- [ ] 1.6b Dashboard render tests (jsdom + testing-library): topology controls default off, localStorage persistence, chip feedback without scrolling — review of #49; macOS gains a SwiftPM test target here (producer serialization: `builtIn`, findings/analysis shapes)
- [ ] 1.7 Regenerate all shipped fixtures from current producers; delete stale `surface-chain.v1.json`
