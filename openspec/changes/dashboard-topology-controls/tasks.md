# Tasks: dashboard-topology-controls
- [ ] 1.1 `docs/schema-v1.md`: `nodes[].builtIn?` (producer classification; view decides)
- [ ] 1.2 Producers: Windows sets `builtIn` from `DeviceFilters`; macOS marks Apple internal keyboard/trackpad/camera/ALS and the internal display's hub — tests
- [ ] 1.3 Dashboard: mode radio → Physical / All device nodes; feedback chip with folded/surfaced counts — issue #42
- [ ] 1.4 Dashboard: Include built-in devices toggle (default off, persisted), "N built-in hidden" chip, inspector shows `builtIn` — issue #43
- [ ] 1.5 Tests: below-the-fold mode switch; Surface-class built-in filter keeps LG + TB4 dock branches
- [ ] 1.6 READMEs: Windows README's stale "Include built-in devices" sentence becomes true again
